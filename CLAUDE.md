
## Mandatory project context

Before implementing any task, read:

- `docs/project_brief.md`
- the current task file under `tasks/`

You are the implementation worker, not the supervisor.
Do not reinterpret the project scope beyond the task file.
Do not access `.env`, `secrets`, `data`, `datasets`, `checkpoints`, or `outputs`.
